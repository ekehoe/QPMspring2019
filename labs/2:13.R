set.seed (123)
n.samples = 1000
##you get 1000 means, for each sample you do it 20 times
sample.size = 20
# make an empty vector; this sets parameters

x.bars = rep(NA, n.samples)

for (variable in vector) {
  
}

##not finishing this